# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js build output
.next/
out/

# Production build
build/
dist/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Stray npm lockfile — this is a pnpm project (pnpm-lock.yaml is the source of
# truth per CLAUDE.md). A package-lock.json makes Vercel's package-manager
# detection ambiguous and re-triggers the workspace-root warning.
package-lock.json

# Leftover Claude Security scan output (self-ignored, kept out of the tree)
CLAUDE-SECURITY-*/

# Claude Code
.claude/

# V0 export folders
emt-staffing-marketplace-personnel-page/
.vercel

# Local-only context / planning / strategy / spec docs — NEVER on the public repo.
# Everything private lives in _private/ so ONE rule covers it. A per-file list is how
# STANDBY-IMPROVEMENTS.md and the mis-cased logo slipped through before — keep new
# private docs inside _private/ rather than adding filenames here.
_private/

# Design source-of-truth asset kept at repo root (see CLAUDE.md "Brand"); untracked,
# not for the public repo. Exact filename — note the capital B in "StandBy".
StandBy Logo.png

# Separate sub-project (own lockfile, not a workspace)
pointer-ai-landing-page/
