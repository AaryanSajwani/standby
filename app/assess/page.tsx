import { AssessmentIntakeForm } from "@/components/assessment/AssessmentIntakeForm"

export const metadata = {
  title: "New Assessment — Standby",
}

export default function AssessPage() {
  return <AssessmentIntakeForm />
}
